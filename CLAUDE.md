# Supplever — הקשר לפרויקט

> קובץ זה נטען אוטומטית כהקשר בכל שיחה על הפרויקט. מטרתו לתת תמונה מלאה של מה שאנחנו בונים, למי, ולמה — כדי שלא נצטרך להסביר הכל מחדש בכל פעם.

## מה זה Supplever?

אפליקציה שעוקבת אחרי **מלאי תרופות** של חולים כרוניים, מחשבת כמה ימים נשארו עד שהמלאי ייגמר, ומתריעה **מספיק זמן מראש** כדי להזמין בלי לחץ ובלי הפתעות.

**הבעיה שהיא פותרת:** הרגע שכל מטופל כרוני מכיר — היד נשלחת לקופסת התרופות, והיא ריקה.

**קהל היעד:**
- חולים כרוניים
- מבוגרים
- בני משפחה שמטפלים באדם אחר ומנהלים עבורו את המלאי
- כל מי שלא רוצה להסתמך על הזיכרון כשמדובר בתרופות

## שלב נוכחי

**MVP בפיתוח פעיל — כבר עובד בדפדפן, לא רק קונספט.** בנוסף לעמוד הקונספט [supplever-cover.html](supplever-cover.html) (שנשאר כעמוד נחיתה נפרד ומגדיר את השפה העיצובית), קיימת אפליקציה עובדת:

**האפליקציה חיה בפועל, ציבורית, עם HTTPS:** [alongut291-arch.github.io/Supplever](https://alongut291-arch.github.io/Supplever/) — מתארחת חינם ב-GitHub Pages, מהריפו [github.com/alongut291-arch/Supplever](https://github.com/alongut291-arch/Supplever). כל עדכון דורש `git push` ל-branch `main` (אין דיפלוי אוטומטי מתוחכם מעבר לזה — GitHub Pages בונה מחדש אוטומטית מה-branch).

### חשוב מאוד: איך לשלוח עדכון כדי שהוא באמת יגיע למשתמשים שכבר התקינו את ה-PWA

> ⚠️ **הפרק הזה מכסה את גרסת האתר בלבד.** מי שהתקין מ-Google Play מריץ אפליקציית Capacitor שנושאת **עותק משלה** של הקבצים, ו-`git push` לא מגיע אליו כלל. לעדכון שלו נדרשת בנייה והעלאה חדשה — ראו **"שני מקומות, לא אחד"** בפרק Google Play למטה. **כל שינוי קוד דורש את שני המסלולים.**

בגלל שיש Service Worker עם cache-first לעבודה אופליין ([sw.js](sw.js)), **git push לבד לא מספיק** כדי שמשתמש עם האפליקציה כבר מותקנת יראה את השינוי. חובה **בכל עדכון קוד** (גם שינוי קטן ב-app.js/style.css/index.html):
1. לעדכן את מספר הגרסה במשתנה `CACHE_NAME` בראש [sw.js](sw.js) (למשל `supplever-v1` → `supplever-v2`).
2. רק אז git add/commit/push.

בלי זה, הדפדפן לא "מבין" שיש גרסה חדשה (הוא בודק אם sw.js עצמו השתנה, לא אם app.js השתנה) וימשיך להגיש למשתמש את הגרסה הישנה מה-cache לנצח. גם אחרי עדכון תקין, המשתמש עשוי לראות את הגרסה החדשה רק בפתיחה השנייה של האפליקציה (לא בהכרח הראשונה) — זו התנהגות תקנית של Service Workers, לא באג.

| קובץ | תפקיד |
|---|---|
| [index.html](index.html) | מבנה האפליקציה: 3 טאבים (המלאי שלי / דורש הזמנה / רשימת ההזמנות), מודלים |
| [style.css](style.css) | עיצוב האפליקציה, לפי אותם טוקנים כמו עמוד הקונספט |
| [app.js](app.js) | כל הלוגיקה: localStorage, חישוב ימים, סטטוס, התראות |
| [sw.js](sw.js) | Service Worker — cache-first לעבודה אופליין + system notifications |
| [manifest.json](manifest.json) | הגדרות PWA (שם, אייקונים, צבעים, מצב standalone) |
| [icons/](icons) | אייקוני PWA (192/512/apple-touch) — **טבעת הקפסולות המלאה**, זהה ללוגו, לא כמוסה בודדת. נוצרים ע"י [.claude/make-icons.ps1](.claude/make-icons.ps1) |
| [.claude/launch.json](.claude/launch.json), [.claude/serve.ps1](.claude/serve.ps1) | שרת פיתוח מקומי ב-PowerShell (ראו הערה למטה) |

**פיצ'רים שכבר ממומשים ונבדקו בדפדפן:** הוספת/עריכת/מחיקת תרופה, חישוב אוטומטי של ימים שנותרו, סטטוס צבעוני (ירוק/צהוב/אדום), מיון לפי דחיפות, עדכון מלאי מהיר אחרי קבלת הזמנה, התראת מערכת בפתיחת האפליקציה כשיש תרופה דחופה (עם הגנה מפני ספאם — פעם ביום לכל תרופה), התקנה כ-PWA, ועבודה אופליין אמיתית (נבדק ידנית: האפליקציה נטענת גם כששרת הפיתוח כבוי).

**שלושה טאבים:**
1. **המלאי שלי** — כל התרופות.
2. **דורש הזמנה** — מסונן אוטומטית לפי סטטוס (לא ניתן לעריכה ידנית של מי נמצא כאן). לכל תרופה יש **תכנון כמות הזמנה**: בחירת חודש/חודשיים/3 חודשים קדימה, והמערכת מציגה כמה קופסאות/יחידות צריך להזמין כדי לכסות את כל התקופה שנבחרה (חישוב לפי קצב הצריכה בלבד — **בכוונה לא מקזז** את המלאי הקיים, כדי לא להציג "אין צורך להזמין" לתרופה שבכלל נמצאת ברשימה הזו; הבחירה של מספר החודשים היא זמנית לכל ביקור, לא נשמרת בין רענונים). כפתור **"+ הוסף להזמנה"** מוסיף את התרופה לטאב השלישי.
3. **רשימת ההזמנות** — "עגלת קניות" ידנית: התרופות שהמשתמש בחר במפורש להוסיף (שדה `inOrder` על התרופה). מוצגת כ**רשימה רזה** (שורה קצרה: שם · מינון · כמות קופסאות), לא ככרטיס מלא — בכוונה, לפי בקשת המשתמש. כמות הקופסאות היא **תמונת מצב קפואה** מרגע הלחיצה על "הוסף להזמנה" (שדות `orderSnapshotBoxes`/`orderSnapshotPills`) — לא מתעדכנת יותר אם המשתמש חוזר ומשנה את בורר החודשים ב"דורש הזמנה" לאותה תרופה. יש כפתור הסרה (✕). ברגע שמעדכנים מלאי (קיבלתי הזמנה), `inOrder` מתאפס אוטומטית — ההנחה היא שההזמנה מולאה. זו הרשימה שמיועדת בפועל לקחת לבית המרקחת (בשונה מ"דורש הזמנה" שהיא רק אינדיקציה אוטומטית של מה דחוף, ועדיין ניתנת לעריכה חופשית). יש כפתור **"שליחת רשימת ההזמנות"** (ראו סעיף הבא).

### שליחת רשימת ההזמנות במייל — דרך `mailto:`, לא שרת

מכיוון שאין backend ב-MVP (ראו "החלטות מוצר"), שליחת המייל **אינה שליחה אוטומטית אמיתית** — הכפתור בונה קישור `mailto:` (עם נושא וגוף מוכנים מראש, כולל כל התרופות והכמויות מ"רשימת ההזמנות") ופותח אותו, מה שמפעיל את **אפליקציית המייל שכבר מותקנת/מוגדרת במכשיר** עם טיוטה מוכנה — המשתמש עדיין צריך ללחוץ "שליחה" בעצמו שם. זו לא מגבלה זמנית אלא הדרך היחידה שאפשר לשלוח מייל בלי שרת/API חיצוני, ותואמת לפילוסופיית local-first של הפרויקט.

כתובת המייל של המשתמש **נשמרת ב-localStorage** (`supplever_user_email`), לא נדרשת בפתיחה הראשונה של האפליקציה אלא רק **בפעם הראשונה שלוחצים "שליחת רשימת ההזמנות"** (מודל קליטה קטן, `emailModalOverlay`/`emailForm` ב-[index.html](index.html)). אחרי השמירה הראשונה, לחיצות הבאות על הכפתור שולחות ישירות בלי לשאול שוב. אפשר לשנות את הכתובת בכל רגע דרך קישור "שינוי" שמופיע מתחת לכפתור.

**עוד לא ממומש מה-MVP:** ריבוי מטופלים/פרופילים במכשיר אחד (עדיין החלטה פתוחה).

### הערה טכנית חשובה: אין Python/Node על המחשב הזה

בבדיקה התברר ש-Python ו-Node.js **לא מותקנים בפועל** על המחשב (רק קיצורי דרך של Microsoft Store שלא עובדים). לכן שרת הפיתוח המקומי כתוב ב-PowerShell טהור ([.claude/serve.ps1](.claude/serve.ps1)), ורץ דרך `.claude/launch.json` על פורט 5500. חשוב לזכור את זה לפני שמציעים כלים שדורשים npm/pip.

### תדירות נטילה — שלושה מצבים, מספר אחד מתחת למכסה

לא כל תרופה נלקחת כל יום. הטופס מציע שלושה מצבים (`med.freqMode`):

| מצב | שדות | שדות ב-med |
|---|---|---|
| `daily` (ברירת מחדל) | יחידות ביום | `dailyRate` |
| `weekly` | יחידות בכל נטילה + פעמים בשבוע | `dailyRate`, `timesPerWeek` |
| `cycle` | יחידות ביום + ימים נוטלים + ימי הפסקה + תאריך תחילת המחזור | `dailyRate`, `cycleOnDays`, `cycleOffDays`, `cycleStart` |

**עיקרון התכנון:** `med.dailyRate` הוא תמיד "יחידות ביום שבו באמת נוטלים", בכל שלושת המצבים. הפונקציה `avgDailyRate(med)` מתרגמת כל מצב ל**מספר אחד** — ממוצע יחידות ליום — וזה המספר שכל שאר האפליקציה (סטטוס, מיון, כמות הזמנה) ממשיכה לעבוד איתו בלי שינוי. **תרופה שנשמרה לפני הפיצ'ר הזה** אין לה `freqMode`, ולכן היא נקראת כ-`daily` והחישוב עבורה זהה לחלוטין למה שהיה — אין צורך במיגרציה של הנתונים.

**חריג אחד:** במצב `cycle`, `daysRemaining` **לא** משתמש בממוצע אלא ב-`cycleDaysRemaining` — לולאה שהולכת יום-יום קדימה ומורידה יחידות רק בימי נטילה. הסיבה: ממוצע לבדו לא יודע אם המטופל נמצא כרגע באמצע הפסקה, ואז הוא מציג **יותר ימים ממה שיש באמת** — הכיוון המסוכן באפליקציה שכל תפקידה להתריע מראש. `cycleStart` הוא מה שמאפשר את הדיוק הזה, ובלעדיו נופלים חזרה לממוצע.

בכרטיס מוצג `freqText(med)` — תיאור אנושי ("פעם בשבוע", "מחזורי · 30 יום נוטלים, 30 יום הפסקה") ולא מספר עשרוני חסר משמעות כמו "0.14 ליום" — ולתרופה מחזורית מתווספת תגית `cycleChipHTML(med)` שאומרת אם היא כרגע בנטילה או בהפסקה ומתי המצב מתחלף.

### ירידת המלאי לאורך זמן — מחשבים, לא מעדכנים

עד ספטמבר 2026 היה באג יסודי: `currentStock` נכתב **רק** כשהמשתמש שמר את הטופס או לחץ "קיבלתי הזמנה", ושום דבר לא הוריד ממנו יחידות עם הזמן. המשמעות הייתה שהספירה קפואה — תרופה עם 30 יחידות ליום אחד הציגה "30 ימים נותרו" גם שבועיים אחר כך, ושום תרופה לא עברה ל"דורש הזמנה" מעצמה. כלומר ההבטחה המרכזית של האפליקציה לא עבדה בפועל.

**המבנה אחרי התיקון:** `med.currentStock` הוא המלאי שהיה בתאריך `med.stockDate` — **לא** המלאי של היום. את המלאי הנוכחי מחשבים כל פעם מחדש דרך `effectiveStock(med)`, ולא מעדכנים את המספר השמור.

זו ההחלטה החשובה כאן, ואסור לשנות אותה ל"פשוט להוריד מהמלאי בכל פתיחה": חישוב הוא **אידמפוטנטי** — פתיחה חוזרת של האפליקציה, רענון, או שני טאבים פתוחים בו-זמנית לא מורידים מלאי פעמיים. גישה שמעדכנת את המספר השמור הייתה שוברת בדיוק שם, ובשקט.

המספר השמור משתנה רק בשתי נקודות, ובשתיהן `stockDate` מתעדכן להיום: שמירת הטופס, וקבלת הזמנה. בקבלת הזמנה מוסיפים ל-`effectiveStock(med)` ולא ל-`med.currentStock`, אחרת הצריכה שכבר קרתה מאז השמירה האחרונה נספרת פעמיים.

במצב `cycle` הצריכה נספרת לפי **ימי הנטילה שבאמת עברו** ולא לפי ממוצע, כדי שמטופל שהיה בהפסקה לא "יאבד" מלאי שכלל לא לקח.

**מיגרציה:** תרופה שנשמרה לפני התיקון אין לה `stockDate`, ולכן `anchorMissingStockDates()` מעגן אותה ל**היום** ולא לתאריך שבו נוספה — אחרת היה נמחק לה מלאי רטרואקטיבית על תקופה שבה ממילא לא הייתה ירידה אוטומטית והמשתמש עדכן ידנית.

### מגבלת התראות — לזכור תמיד

**המנגנון היחיד שאפשר לסמוך עליו הוא ההתראה בפתיחת האפליקציה** (אם יש תרופה דחופה) — זו החלטה מודעת ומוסכמת, לא באג. **אסור להבטיח למשתמש הקצה "תזכורת שתקפוץ מעצמה"** בלי שהאפליקציה נפתחה. בספטמבר 2026 נוסף גם ניסיון לבדיקה ברקע (ראו למטה), אבל הוא best-effort בלבד ואינו משנה את הכלל הזה.

**מה שנבדק ונפסל (ספטמבר 2026):** API בשם **Notification Triggers**, שהיה אמור לאפשר תזמון התראה עתידית ללא שרת — **גוגל נטשה את הפיתוח שלו**. אין תחליף. האפשרות האמינה היחידה נותרה **Web Push**, והיא מחייבת שרת ולכן גם עדכון של הצהרת Data Safety ושל מדיניות הפרטיות (ראו הפרק על Google Play).

**באג פלטפורמה שהתגלה ב-iOS:** ב-Android, ברגע שהמשתמש בוחר "הפעלת התראות" ומאשר, `Notification.permission` נשאר `'granted'` לצמיתות והבאנר לא חוזר. ב-iOS (PWA שהותקן למסך הבית) יש תקלה מוכרת ב-WebKit שבה `Notification.permission` לפעמים **לא נשמר נכון** בין פתיחות — הבאנר "הפעילו התראות" חוזר להופיע גם אחרי שהמשתמשת כבר אישרה, כי הדפדפן "שוכח" את ההרשאה (ייתכן שקשור גם למחיקה/הוספה מחדש של קיצור הדרך למסך הבית, ראו את ההערה על אייקון מסך הבית למעלה). **הפתרון שיושם:** בנוסף לבדיקת `Notification.permission`, נשמר דגל נפרד ב-localStorage (`supplever_notify_requested`) ברגע שהמשתמש לוחץ "הפעלת התראות" — כך שהבאנר לא חוזר שוב **גם אם** iOS "שוכח" את ההרשאה בפועל. זה פותר את הנידנוד החוזר, אבל **לא** יכול להבטיח שההתראות בפועל יעבדו אם iOS באמת לא שומר את ההרשאה ברמת המערכת — זו מגבלת פלטפורמה שאין לנו שליטה עליה מהקוד.

### בדיקת מלאי ברקע — Periodic Background Sync, ולמה היא תוספת בלבד

Chrome יודע להעיר Service Worker מדי פעם (`periodicsync`), וזו הדרך היחידה להתריע כשהאפליקציה סגורה **בלי שרת** — כלומר בלי ששום מידע רפואי עוזב את המכשיר.

**החולשה המובנית, וצריך להכיר אותה לפני שנשענים על זה:** Chrome מחליט בעצמו אם ובאיזו תדירות להעיר, **לפי כמה שהמשתמש פותח את האפליקציה**. בתיעוד של גוגל כתוב שאפליקציה שכמעט לא נפתחת עלולה לקבל מעט אירועים או אפס. ב-Supplever זה הפוך בדיוק: **מי שזקוק להתראה הוא מי שלא פותח**. לכן זו תוספת ולא תחליף, ואין בממשק שום טקסט שמבטיח תזכורת שתגיע מעצמה.

**איך זה בנוי — והעיקרון החשוב:** Service Worker **לא יכול לקרוא `localStorage`**. הפיתוי הוא להעתיק את חשבון המלאי ל-[sw.js](sw.js), וזה היה יוצר **שני עותקים של אותה מתמטיקה** שנפרדים בשקט אחרי התיקון הראשון שנעשה רק באחד מהם. במקום זה:

1. `writeAlertPlan()` ב-[app.js](app.js) רץ בסוף כל `render()` ומחשב לכל תרופה את **התאריך** שבו תחצה לסף ההתראה.
2. הרשימה (`שם` + `תאריך` בלבד) נשמרת ב-Cache Storage, שכן נגיש ל-SW.
3. ה-SW רק **משווה תאריכים**. אפס לוגיקת מלאי בתוכו.

**למה תאריך מוחלט ולא ספירה לאחור:** ירידת המלאי דטרמיניסטית, ולכן התאריך נשאר נכון גם אם האפליקציה לא נפתחה חודשיים. הדבר היחיד שפוסל אותו הוא שינוי במלאי — וכל שינוי כזה עובר דרך `render()`, כלומר התוכנית נכתבת מחדש באותו רגע. ספירה לאחור, לעומת זאת, הייתה נעשית שקרית בכל יום שעובר.

⚠️ **מלכודת שכמעט עלתה לנו בפיצ'ר:** ה-`activate` ב-[sw.js](sw.js) מוחק **כל** מטמון שאינו הגרסה הנוכחית. תוכנית ההתראות היא **נתוני משתמש ולא קובץ אפליקציה**, ולכן היא לא נושאת מספר גרסה — ובלי חריגה מפורשת היא הייתה נמחקת **בכל עדכון גרסה**, ומשביתה את הפיצ'ר עד הפתיחה הבאה של האפליקציה. הקבוע `PLAN_CACHE` מוחרג במפורש מהמחיקה. **כל מטמון עתידי שמחזיק נתוני משתמש חייב אותה חריגה.**

**מה שלא ניתן לבדוק:** אי אפשר לאלץ את Chrome לירות `periodicsync`. ההרשאה גם יוצאת `denied` בטאב דפדפן רגיל — היא דורשת אפליקציה **מותקנת**. לכן נשמר דגל שקט ב-localStorage (`supplever_bgsync_status`) עם תוצאת ההרשמה, כדי שאפשר יהיה לאבחן בדיעבד במכשיר אמיתי. **עדיין לא ידוע אם זה עובד בתוך TWA** — התיעוד מדבר על PWA מותקן, וזה לא בהכרח אותו דבר.

### מלכודת: חלון שנפתח מעל חלון אחר שובר את זיהוי כפתור ה-back

באג אמיתי שנוצר בספטמבר 2026 כשנוסף חלון אישור המחיקה מעל חלון עריכת התרופה: **לחיצה על "ביטול" סגרה גם את חלון העריכה.**

הסיבה עדינה. סגירת חלון קוראת ל-`history.back()`, וה-`popstate` שנוצר מזה **נראה זהה לחלוטין ללחיצת back אמיתית של המשתמש**. הקוד הבחין ביניהם לפי שאלה אחת — "האם נשאר חלון פתוח?" — בהנחה שאם כן, זו הייתה לחיצת משתמש. **ההנחה נכונה רק כל עוד חלון אחד פתוח בכל רגע.** ברגע שחלון אחד נפתח מעל אחר, סגירת העליון מייצרת popstate בזמן שהתחתון עדיין פתוח, והקוד "סוגר" גם אותו.

**הפתרון:** דגל מפורש `closingDialogOurselves` שנדלק ב-`registerDialogClose()` לפני `history.back()`, וה-`popstate` יוצא מיד אם הוא דלוק. **אין דרך להסיק את זה ממצב ה-DOM — חייבים לסמן את זה במפורש.**

**ובנוסף:** לחיצת back אמיתית מעל חלון מוקפץ סוגרת **רק את העליון** ומחזירה לחלון שמתחתיו, ולא את כולם.

**לכל חלון חדש שנוסיף צריך:** להוסיף אותו ל-`anyDialogOpen()`, לקרוא ל-`registerDialogOpen()` בפתיחה ול-`registerDialogClose()` בסגירה, ולוודא ב-`popstate` שהוא נסגר לפי סדר הערימה הנכון. **ולבדוק שהמונה `dialogHistoryEntries` חוזר ל-0** אחרי כל מסלול — מונה שנתקע גורם לכך שהמשתמש צריך שתי לחיצות back כדי לצאת.

### אייקון "מסך הבית" מתעדכן לאט יותר מכל שאר האפליקציה

יש הבדל בין **הלוגו בתוך האפליקציה** (מתעדכן דרך ה-Service Worker, ראו "חשוב מאוד: איך לשלוח עדכון" למעלה — צריך שתי פתיחות) לבין **אייקון מסך הבית** (מה שמותקן כקיצור דרך על הטלפון, מתוך `icons/`). האייקון הזה נשמר ב-cache **של מערכת ההפעלה של הטלפון עצמו** (לא קשור ל-Service Worker שלנו בכלל) — גם אחרי שהקובץ מתעדכן בשרת, לרוב **לא מספיק לפתוח את האפליקציה מחדש**; המשתמש צריך למחוק את הקיצור ממסך הבית ולהוסיף אותו שוב כדי לראות אייקון מעודכן. חשוב להסביר את זה למשתמש במפורש כשעורכים את האייקונים, ולא להניח שזה "יתעדכן לבד" כמו שאר הקבצים.

### מלכודת CSS: `hidden` לא עובד על אלמנט שיש לו `display` ב-CSS

באג אמיתי שהתגלה בספטמבר 2026 ורץ באפליקציה זמן מה בלי שאף אחד שם לב: `el.hidden = true` **לא הסתיר כלום** על הבאדג'ים האדומים של הטאבים ועל כותרות הקבוצות ב"רשימת ההזמנות" ו"דורש הזמנה". התוצאה הייתה באדג' אדום עם "0" שנשאר על הטאב, וכותרת קבוצה ריקה שהופיעה.

הסיבה: הדפדפן מגדיר `[hidden]{display:none}` כברירת מחדל, אבל **כל כלל CSS של הדף שמגדיר `display` גובר עליו** — כללי הדף תמיד גוברים על כללי הדפדפן, בלי קשר לספציפיות. כך ש-`.tab-badge{display:flex}` ו-`.section-label{display:flex}` ביטלו את `hidden` בשקט, בלי שגיאה.

**הפתרון שיושם:** כלל אחד גורף בראש [style.css](style.css) — `[hidden]{ display:none !important; }`. עדיף על הוספת `.something[hidden]` לכל מחלקה חדשה, כי אחרת המלכודת חוזרת בכל פעם שמוסיפים מחלקה עם `display`.

**איך לבדוק את זה נכון:** לא מספיק לבדוק `el.hidden === true` ב-JS — זה יחזיר `true` גם כשהאלמנט נראה על המסך. צריך לבדוק `getComputedStyle(el).display === 'none'` או `el.getBoundingClientRect().height === 0`. הבדיקה השגויה הזו היא הסיבה שהבאג לא נתפס בזמנו.

### מלכודת PowerShell: מספרים שליליים בקריאה לפונקציה משלנו

ב-[.claude/make-icons.ps1](.claude/make-icons.ps1) התגלה באג אמיתי: קריאה לפונקציית PowerShell (לא פקודת .NET) בסגנון `Get-RoundedPillPath -7 -3.5 14 7 3.5` (בלי סוגריים/פסיקים) גורמת ל-PowerShell לקשור את `-7` כ**מחרוזת** `"-7"` ולא כמספר — מה שהופך חיבור/חיסור אריתמטי בהמשך לשרשור מחרוזות שקט, בלי שגיאה, ומייצר קואורדינטות הזויות (הבאג גרם ללוגו הטבעתי להיראות כמו כוכב שבור). הפתרון: **תמיד** להצהיר טיפוסים מפורשים בפרמטרים של פונקציות PowerShell עצמיות שמקבלות מספרים (`[double]$x` ולא סתם `$x`), במיוחד כשעלולים לשלוח להן ערכים שליליים.

### מלכודת PowerShell נוספת: הפסיק `,` "חזק" יותר מ-`+`/`-`

עוד באג אמיתי מאותה עבודה: ביטוי כמו `@($a + $x, $b + $y)` בתוך `@(...)` **לא** מתפרש כמצופה כ-`(a+x),(b+y)` — ב-PowerShell לאופרטור הפסיק `,` יש עדיפות **גבוהה יותר** מ-`+`/`-`, כך שזה בפועל מתפרש כ-`$a + ($x, $b) + $y` (ניסיון לחבר מספר למערך), וזורק שגיאת `op_Addition`/`op_Subtraction` לא ברורה על `System.Object[]`. הפתרון: לעטוף כל תת-ביטוי אריתמטי בסוגריים משלו בתוך `@(...)`, למשל `@(($a + $x), ($b + $y))`.

### אפליקציית אנדרואיד ב-Google Play — Capacitor, לא TWA

**הקוד לא נכתב מחדש.** מה שמותקן מהחנות הוא אותו HTML/CSS/JS, **ארוז בתוך אפליקציית אנדרואיד אמיתית** ([Capacitor](https://capacitorjs.com/)) שמריצה אותו ב-WebView משלה, מקבצים שיושבים בתוך החבילה. פרויקט הבנייה יושב ב-`C:\Users\along\SuppleverApp`, **מחוץ לריפו**.

בספטמבר 2026 הגרסה הראשונה הופצה כ-**TWA** — מעטפת דקיקה שפתחה את האתר בכרום. היא הוחלפה כי הביאה איתה את כל בעיות הכרום: שורת כתובת שחוזרת, "running in Chrome", מסך פתיחה לבן, ותלות במטמון של דפדפן חיצוני. נקודת החזרה שמורה ב-tag **`twa-final`** ובענף **`twa-final-branch`**.

#### ⚠️ ההשלכה החשובה ביותר לתחזוקה: שני מקומות, לא אחד

**ב-TWA `git push` הספיק — הוא עדכן גם את משתמשי החנות. ב-Capacitor זה כבר לא נכון**, כי לאפליקציה יש **עותק משלה** של הקבצים.

| מה עשית | מי מקבל את השינוי |
|---|---|
| `git push` בלבד | רק מי שנכנס דרך הדפדפן (PWA/אתר) |
| בנייה + העלאה ל-Play | **משתמשי החנות** |

**כל שינוי קוד דורש את שניהם.** התסריט שקל ליפול אליו: לתקן באג, לדחוף, לראות שהאתר תקין, ולהיות בטוח שסודר — בזמן שכל משתמשי החנות ממשיכים לראות אותו.

הסדר המלא לכל שינוי:

1. לעדכן `CACHE_NAME` ב-[sw.js](sw.js) — הכלל מלמעלה עדיין תקף, לגרסת האתר
2. `git add` / `commit` / `push`
3. להעתיק `app.js`, `index.html`, `style.css` (וכל קובץ שהשתנה) אל `SuppleverApp\www\`
4. `npx cap sync android`
5. `./gradlew bundleRelease` → העלאה ל-Play

**`sw.js` לא מועתק ל-`www` בכוונה** — Service Worker מיותר באפליקציה ארוזה (הקבצים כבר מקומיים) ורק מוסיף שכבת מטמון שתגיש קוד ישן. `registerServiceWorker()` ב-[app.js](app.js) יוצא מוקדם כשהוא מזהה שהוא רץ בתוך האפליקציה.

#### פרטים שננעלו ואי אפשר לשנות

| | |
|---|---|
| Package ID | `app.supplever.android` — **ננעל לתמיד** ברגע יצירת האפליקציה ב-Play Console |
| חשבון מפתח | `supplever.app@gmail.com` — חשבון **פרטי** (25$ חד-פעמי) |
| קטגוריה | **Health & Fitness** · שפת ברירת מחדל: עברית · קהל יעד: 18+ — ראו "חשבון פרטי לא יכול לפרסם אפליקציה רפואית" למטה. **אין לשנות ל-Medical.** |
| מודל | Free — **בלתי הפיך**. אפליקציה חינמית לא ניתן להפוך לבתשלום לעולם (מונטיזציה עתידית אפשרית רק דרך רכישות בתוך האפליקציה, שעובדות גם באפליקציה חינמית) |

#### מפתח החתימה

`signing.keystore` ו-`signing-key-info.txt` (עם הסיסמאות) יושבים בתיקייה `מפתח חתימה - גיבוי קריטי`, **במקביל לתיקיית הפרויקט ובכוונה מחוץ לריפו**. ב-[.gitignore](.gitignore) נחסמו `*.keystore`, `*.jks`, `signing-key-info.txt`, `*.aab` ו-`*.apk` — **הריפו ציבורי**, ומפתח שנדחף אליו פעם אחת נחשף לצמיתות.

מכיוון ש-Play App Signing מופעל, המפתח הזה הוא **מפתח העלאה בלבד**: אם הוא אובד, גוגל יכולה לאפס אותו דרך פנייה לתמיכה. זה לוקח ימים, ולכן הגיבוי עדיין חשוב — אבל זו לא קטסטרופה בלתי הפיכה כמו שזה היה בלי המנגנון הזה.

#### סביבת הבנייה — שלושה דברים שבלעדיהם זה לא מתקמפל

| | |
|---|---|
| `JAVA_HOME` | `C:\Users\along\SuppleverApp\.jdk\jdk-21.0.12.1+1` |
| `ANDROID_HOME` | `%LOCALAPPDATA%\Android\Sdk` |
| Node | `C:\Program Files\nodejs` — **חייב להיות מוקדם ב-PATH בכל פקודה** |

**למה JDK 21 ולא מה שמותקן:** אנדרואיד סטודיו מגיע עם Java 25, ו-Gradle 8.14.3 תומך עד Java 24. התוצאה היא `Unsupported class file major version 69` — שגיאה שלא מרמזת כלל על מה הבעיה. לכן הורד Temurin JDK 21 ייעודי לתוך הפרויקט.

**למה הפרויקט ב-`C:\Users\along\SuppleverApp` ולא ליד הקוד:** **Gradle נשבר על נתיבים שאינם ASCII**, ונתיב הפרויקט מכיל `עבודה`. זו לא העדפה — זו הסיבה היחידה. אל תעביר אותו.

⚠️ **ולא להשאיר קבצי גיבוי בתוך `res/`.** קובץ בשם `styles.xml.bak` מפיל את הבנייה כולה עם `The file name must end with .xml`. זה קרה בפועל.

#### ההגירה מוחקת את כל נתוני המשתמשים — וזה לא מובן מאליו

`localStorage` שייך ל**מקור בתוך אפליקציה מסוימת**. ב-TWA הנתונים ישבו במאגר של **כרום**; ב-Capacitor הם יושבים ב-WebView של האפליקציה, תחת `https://localhost`. **אין גשר ביניהם.** כל מי שעדכן קיבל אפליקציה ריקה.

**לכן נבנה גיבוי/שחזור לקובץ** (`buildBackup()` / הייבוא ב-[app.js](app.js), מסך ההגדרות). **סדר הפעולות הוא שקבע הכול:** הגיבוי חייב להגיע למשתמשים **לפני** שהגרסה החדשה מגיעה אליהם. הפוך — והם מאבדים הכול בלי דרך לשחזר.

**הכלל הכללי:** כל שינוי עתידי שמזיז את הדומיין, את אופן האריזה או את מנוע הרינדור — **מוחק את הנתונים בשקט**. לבדוק את זה לפני, לא אחרי.

#### מלכודות של "אנדרואיד מצייר, לא אנחנו"

כולן נובעות מאותו שורש: **חלק מהמסך אינו HTML שלנו.** ב-TWA כרום טיפל בהן והן היו בלתי נראות; ברגע שהאפליקציה עומדת בפני עצמה, הן צפות.

**1. כפתור "אחורה" — ל-Capacitor אין טיפול בו. בכלל.**
חיפוש בקוד המקור של Capacitor 8 מחזיר **אפס** התייחסויות. ברירת המחדל של אנדרואיד היא לסגור את המסך, ולכן לחיצה על back סגרה את האפליקציה גם כשחלון היה פתוח — כל מנגנון ה-`pushState`/`popstate` שלמעלה מעולם לא קיבל את האירוע. הפתרון: רכיב `@capacitor/app`, והאזנה ל-`backButton` ב-`setupNativeBackButton()`. **עצם הרישום מבטל את ברירת המחדל**, ואז מתרגמים את הלחיצה ל-`history.back()` — אותו דבר שהדפדפן היה עושה לבד. כך הלוגיקה נשארת עותק אחד לשני העולמות.

**2. ערכת נושא `DayNight` צובעת בלבן את חלונות המערכת.**
תבנית Capacitor מגדירה `Theme.AppCompat.DayNight`, שמתהפכת לפי הגדרת הטלפון. בטלפון במצב כהה הטקסט בחלונות של `<select>` ושל בוחר התאריך נצבע **לבן** — בעוד הדף עצמו נשאר בהיר, כי ב-[style.css](style.css) אין `prefers-color-scheme`. **האפליקציה בהירה תמיד, ולכן `Theme.AppCompat.Light`.**
⚠️ **ובאותו מקום: `android:background` הוא גורף ודולף לחלונות המערכת.** הוא זה שצבע להם רקע בהיר והשלים את הצירוף הבלתי קריא. למסך עצמו יש `android:windowBackground`, והוא לא נוגע בחלונות אחרים.

**3. הבוררים מצוירים אצלנו, כי אחרת אין להם את הגופן.**
גופן ווב לא קיים מחוץ ל-WebView, ולכן **אין שום הגדרה** שתיתן ל-`<select>` של אנדרואיד את Heebo. לכן `openChoiceDialog()` ב-[app.js](app.js) מצייר את הרשימה בעצמו. **ה-`<select>` האמיתי נשאר בדף** ומחזיק את הערך, נשאר חלק מהטופס ויורה `change` — כך שכל קוד קיים שקורא `.value` או מאזין ל-`change` לא נגע.
המנגנון: `pointer-events:none` על ה-`<select>` (כדי שלעולם לא יקבל את הלחיצה ולא יפתח את חלון המערכת) + עטיפה שמקבלת אותה במקומו. **כל `<select>` חדש נעטף אוטומטית** ע"י `enhanceSelects()`, שרץ גם בסוף `render()` — בורר החודשים נוצר מחדש בכל ציור.
⚠️ **בוחר התאריך עדיין של אנדרואיד.** לוח שנה משלנו הוא עבודה בסדר גודל אחר.

**4. `<label>` שעוטף פקד משכפל כל לחיצה אליו.**
בורר החודשים ישב בתוך `<label>`, ולכן כל לחיצה הגיעה פעמיים: אחת אמיתית, ואחת שה-label העביר אל ה-`<select>` — שממנה היא בעבעה חזרה אל העטיפה. התוצאה: הבורר נפתח פעמיים ונרשמו **שתי** רשומות היסטוריה, כלומר המשתמש היה צריך ללחוץ back פעמיים כדי לצאת. `pointer-events:none` **לא** עוצר את זה, כי זו העברה של ה-label ולא בדיקת פגיעה.
**הבאג נתפס רק כי נמדד `dialogHistoryEntries` אחרי כל מסלול — לא כי משהו נראה שבור.** זו הבדיקה שצריך להריץ על כל חלון חדש.

**5. סרגל הסטטוס: הגדרה בזמן ריצה לבדה נמחקת בסגירת מסך הפתיחה.**
Capacitor קובעת את צבע האייקונים לפי `SystemBars.style` ב-`capacitor.config.json` — אבל **פעם אחת בלבד**, בטעינת הרכיב. ברירת המחדל שלה (`DEFAULT`) הולכת אחרי מצב כהה של **הטלפון**, ולכן בטלפון כהה היא נותנת אייקונים לבנים על הרקע הבהיר שלנו — בלתי קריא. ההגדרה `"style": "LIGHT"` מתקנת את זה, **אבל רק לשבריר שנייה**: סגירת מסך הפתיחה מחילה מחדש את ערכת הנושא, וזו מוחקת כל מה שנקבע בזמן ריצה.
**לכן `android:windowLightStatusBar` חייב להיות מוצהר בערכת הנושא עצמה** — גם ב-`AppTheme.NoActionBar` וגם ב-`AppTheme.NoActionBarLaunch`. ערכת הנושא היא מה שהמערכת חוזרת אליו; הקונפיג הוא רק מה שנקבע בהתחלה. **שניהם ביחד, לא אחד מהם.**
**הכלל הכללי:** כל מאפיין חלון שנקבע בזמן ריצה ולא מוצהר בערכת הנושא — ייעלם ברגע שמשהו מחיל את הערכה מחדש.

#### ⚠️ אין צורך ב-safe-area — Capacitor כבר מרפדת בעצמה

זה כמעט עלה בקוד מת שהיה נראה כמו הגנה. **Capacitor מרפדת את החלון נטיבית**, וה-WebView יושב מתחת לסרגלי המערכת:

```java
boolean shouldPassthroughInsets = webViewMajorVersion >= 140 && hasViewportCover;
```

`hasViewportCover` נקבע לפי האם ב-[index.html](index.html) יש `viewport-fit=cover`. **אין לנו, ולכן false** — Capacitor מריצה את הענף שמרפד את `decorView` **ומזריקה `0px`** ל-`--safe-area-inset-*`.

**כלומר `padding-top:var(--safe-area-inset-top)` מתווסף לאפס ולא עושה דבר.** הרצועה שמאחורי הסרגל היא `android:windowBackground` — ולכן היא בצבע האפליקציה.

**זה ישתנה רק אם נוסיף `viewport-fit=cover` למטא-תג.** אין סיבה לעשות זאת, ואם כן — אז ורק אז הריפוד הופך נחוץ.

#### אייקון ההתראות — בלי הגדרה מקבלים סימן קריאה

ללא `smallIcon` בקונפיג, `@capacitor/local-notifications` נופל ל-`android.R.drawable.ic_dialog_info` של אנדרואיד — סימן קריאה בעיגול. ההגדרה יושבת ב-`capacitor.config.json` תחת `LocalNotifications.smallIcon`, ומצביעה על שם drawable בלי סיומת.

⚠️ **אנדרואיד מתעלם מהצבעים באייקון שורת הסטטוס.** הוא קורא **רק את ערוץ השקיפות** וצובע את הצללית בעצמו. לכן האייקון הוא **צללית לבנה על שקוף** — לא ניתן להציג שם את הכמוסות הצבעוניות. `iconColor` בקונפיג משפיע רק על הגוון **במגירת ההתראות**, לא בשורת הסטטוס.

האייקון נוצר מהגאומטריה של הלוגו (אותן 14 קפסולות, אותן זוויות) ע"י `.claude/make-stat-icon.js`, בחמש צפיפויות. **נבדק בעין על רקע כהה לפני שנשלח** — ב-24 פיקסל הצורה מיטשטשת, וב-72 (רוב הטלפונים) היא ברורה.

#### לחיצה על התראה — האירוע של הרכיב, לא "חזרה לאפליקציה"

הקוד שבוחר את הטאב רץ פעם אחת בטעינת הדף, ולכן פתיחה מהתראה נחתה על "דורש הזמנה" **רק בהפעלה מאפס**. כשהאפליקציה כבר ברקע הדף לא נטען מחדש, והמשתמש חזר לטאב שבו היה.

**הפתרון הוא האירוע `localNotificationActionPerformed` ולא `appStateChange`** — הוא מסמן שהמשתמש ביקש במפורש לראות מה דורש הזמנה. לקפוץ לטאב אחר בכל חזרה לאפליקציה היה מרגיז יותר מהבעיה המקורית.

`appStateChange` כן משמש, אבל **רק לציור מחדש**: חישוב הימים נעשה בזמן הציור, ולכן אפליקציה שישבה ברקע שבוע מציגה מספרים ישנים. ⚠️ **ולא מציירים מתחת לחלון פתוח** — הציור בונה מחדש את הכרטיסים, ובורר שפתוח מעליהם היה נשאר מצביע על אלמנט שכבר לא בדף.

#### ⚠️ קובץ בדיקה על הטלפון חוסם עדכונים מ-Play — לתמיד

APK שנבנה כאן חתום במפתח **ההעלאה**; מה ש-Play מגישה חתום במפתח של **גוגל**, שחותמת מחדש כל גרסה. **אנדרואיד לא מתקין אפליקציה מעל אפליקציה בחתימה אחרת**, וההודעה היחידה שמתקבלת היא `Can't install` בלי הסבר.

**היציאה מזה עוברת דרך הסרה — כלומר מחיקת כל נתוני המשתמש.** הסדר היחיד שאינו מאבד אותם: **לגבות → לוודא שהקובץ ירד → להסיר → להתקין מ-Play → לשחזר.**

**ההחלטה שהתקבלה (ספטמבר 2026): לא להתקין יותר קבצי בדיקה על הטלפון של המשתמש.** כל תיקון עובר דרך Play. זה מאט כל סבב בביקורת שלמה, ולכן **צוברים תיקונים לגרסה אחת** במקום לשלוח אחד-אחד. הבודקים ממילא מעולם לא הושפעו — אצלם הכול הגיע מ-Play מלכתחילה.

#### אייקון האפליקציה — Capacitor מקטינה אותו כברירת מחדל

תבנית Capacitor עוטפת את הציור ב-`<inset android:inset="16.7%">` בתוך `mipmap-anydpi-v26/ic_launcher.xml`, כדי שלא ייחתך ע"י מסכה. התוצאה: הלוגו נראה **קטן מהותית** מגרסת ה-TWA, שבה בכלל לא היה אייקון מסתגל אלא PNG מלא.

**החשבון, אם צריך לכוונן שוב:**

```
חלק נראה = 1.5 × (חלק הציור מתוך הקובץ) × (1 − 2×inset)
```

ה-1.5 הוא היחס בין קנבס האייקון (108dp) לחלק שהמשגר מציג (72dp). בפרויקט הזה הציור תופס **64%** מהקובץ, ולכן `inset=7.3%` נותן **82%** מרוחב האייקון.

**לא למדוד לפי העין** — זה נמדד בפועל בהשוואה בין ה-PNG של ה-TWA לבין קובץ ה-foreground, בכל הצפיפויות.

⚠️ **ואנדרואיד שומר אייקונים במטמון של מערכת ההפעלה.** אחרי עדכון האייקון לרוב **צריך להסיר ולהתקין מחדש** כדי לראות אותו. אייקון שלא השתנה אינו עדות לכך שהתיקון נכשל.

#### התראות: לוותר על דיוק, ולוותר איתו על ההרשאה

`@capacitor/local-notifications` מתזמן כברירת מחדל **התראה מדויקת לשנייה** (`isExactNotification: true`). אנדרואיד 14 **חוסם** את ההרשאה הזו כברירת מחדל לאפליקציות עם `targetSdk 33+`, וכשהיא חסומה **הרכיב פותח למשתמש את מסך ההגדרות של אנדרואיד בכל קריאה ל-`schedule()`**. אצלנו הסנכרון רץ בסוף כל `render()` — כלומר בכל פתיחה ובכל שינוי מלאי.

**לכן מועבר `isExactNotification: false`**, וההרשאה `SCHEDULE_EXACT_ALARM` מוסרת במפורש מהמניפסט (`tools:node="remove"`; הרכיב מצהיר עליה בעצמו). להתראה של "המלאי עומד להיגמר" סטייה של שעה חסרת משמעות.

⚠️ **`isExactNotification` יושב על אובייקט ההתראה, לא בתוך `schedule`.** בתוך `schedule` הוא פשוט מתעלם — בלי שגיאה.

⚠️ **ויש שתי נקודות קריאה, לא אחת.** `syncNativeNotifications` מתזמן מראש, ו-`showSystemNotification` יורה מיד — **שתיהן עוברות דרך אותה קריאה ל-`schedule()`**, והרכיב מחליט אם לפתוח את מסך ההגדרות **לפי כל הקבוצה**, לא לפי השאלה אם ניתן זמן תזמון. התיקון הראשון כיסה רק את הראשונה, והמשתמש עדיין הוקפץ. **הדגל נדרש בשתיהן.**

**`allowWhileIdle: true` כן נשאר** — הוא מה שמעיר את הטלפון גם במצב חיסכון בסוללה.

#### `assetlinks.json` — שתי טביעות אצבע, ולמה זה קריטי

> 🕘 **היסטורי — לא רלוונטי לאפליקציה שבחנות היום.** אפליקציית Capacitor טוענת את הקבצים מתוך עצמה ואינה נוגעת בדומיין, ולכן `assetlinks.json` אינו משפיע עליה כלל. הפרק נשמר למקרה של חזרה ל-TWA (ראו `twa-final`), ומפני שקובץ האימות עדיין מתארח בריפו `alongut291-arch.github.io`.

הקובץ שמסתיר את שורת הכתובת חייב לשבת ב**שורש הדומיין**, לא בתת-תיקייה. מכיוון שהאפליקציה יושבת ב-`alongut291-arch.github.io/Supplever/`, הקובץ מתארח ב**ריפו נפרד לגמרי** — `alongut291-arch/alongut291-arch.github.io` — בנתיב `.well-known/assetlinks.json`.

**הוא מכיל שתי טביעות אצבע, ושתיהן הכרחיות:**

| טביעה | חותמת את |
|---|---|
| מפתח ההעלאה שלנו | ה-APK שמתקינים ידנית לבדיקה |
| מפתח החתימה של גוגל | **מה שהמשתמשים מורידים מהחנות** |

גוגל חותמת מחדש כל גרסה, ולכן טביעת האצבע של מה שיורד מהחנות **שונה משלנו**. בלי השנייה, האפליקציה מהחנות נפתחת עם שורת כתובת גלויה — בלי שום שגיאה שתסביר למה. את טביעת האצבע של גוגל שולפים מ-**Protected with Play → Play Store protection → Manage Play app signing**.

אימות מהיר, בלי לחכות להתקנה:
```
https://digitalassetlinks.googleapis.com/v1/statements:list?source.web.site=https://alongut291-arch.github.io&relation=delegate_permission/common.handle_all_urls
```
**אבל שים לב:** ה-API הזה מאשר שהקובץ קריא ותקין — **לא** שטביעת האצבע נכונה. טעות של תו אחד תעבור אותו בשלום. ההוכחה היחידה היא התקנה מהחנות ובדיקה בעיניים שאין שורת כתובת.

**לעולם לא להעתיק טביעת אצבע מצילום מסך.** זה קרה כאן בפועל: קראתי `85` כ-`B5` מצילום של Play Console, בית אחד מתוך 32 — והתוצאה הייתה שורת כתובת גלויה, בלי שום שגיאה בשום מקום. ה-API החזיר "תקין", כי הקובץ אכן היה תקין; הוא פשוט הצהיר על מפתח שלא קיים.
**הדרך הנכונה:** ב-Play Console, בתחתית עמוד **Manage Play app signing**, יש בלוק **"Digital Asset Links JSON"** עם **כפתור העתקה**. להשתמש בו, ולהשוות את התוצאה לקובץ החי בית-מול-בית לפני שמכריזים שזה עובד.

#### מטמון האימות — למה תיקון הקובץ לא נראה מיד על המכשיר

> 🕘 **רוב הפרק היסטורי** (ראו למעלה). **שתי הפסקאות האחרונות עדיין תקפות במלואן:** הבדיקה במכשיר שני, והכלל שקישור ההצטרפות אינו קישור התקנה.

אנדרואיד בודק את `assetlinks.json` **פעם אחת** ושומר את התוצאה, **והמטמון הזה יושב בדפדפן ברירת המחדל ולא באפליקציה**. לכן אחרי תיקון הקובץ:

- **הסרה והתקנה מחדש של האפליקציה לא מנקה כלום** — התוצאה השלילית נשמרה מחוץ לה.
- מה שכן עוזר: ניקוי מטמון הדפדפן, הפעלה מחדש של הטלפון, או פשוט להמתין (לתוצאה יש תוקף של כשעה).
- ⚠️ **"ניקוי נתונים" של הדפדפן ימחק את כל התרופות של המשתמש** — הן יושבות ב-`localStorage` של הדומיין. **מטמון בלבד, לעולם לא נתונים.**

**הבדיקה שחוסכת שעות ניחושים:** להתקין על **מכשיר אנדרואיד שני**, של מישהו אחר, שמעולם לא ניגש לדומיין. אין בו מטמון, ולכן הוא מראה את המצב האמיתי מיד. כך הוכרע שהתשתית תקינה והבעיה מקומית — אחרי שעה של ניסיונות על מכשיר אחד.

**ולבדיקה כזו צריך קודם להוסיף את הכתובת שלו לרשימת הבודקים.** קישור ההצטרפות אינו קישור התקנה: מי שאינו ברשימה רואה **"הפריט לא נמצא"** בחנות, בלי הסבר. הסדר הוא תמיד: לאסוף Gmail → להוסיף לרשימה ולשמור → להמתין 10-20 דקות → **ורק אז** לשלוח את הקישור.

#### מלכודת: `.nojekyll` בריפו של הדומיין

> 🕘 **היסטורי, מאותה סיבה.**

GitHub Pages מריץ Jekyll, ו-Jekyll **מתעלם מכל תיקייה ששמה מתחיל בנקודה** — כולל `.well-known`. בלי קובץ ריק בשם `.nojekyll` בשורש הריפו `alongut291-arch.github.io`, קובץ האימות פשוט לא יוגש, בשקט מוחלט.

#### מלכודות ב-Play Console שכבר עלו לנו בזמן

- **קוד השפה של עברית הוא `iw` ולא `he`.** ב-Release notes צריך `<iw-IL>…</iw-IL>`; `<he-IL>` נדחה בשגיאה "Unsupported language translation found".
- **"App access" נקרא היום "Sign in details".**
- **הגדרות App integrity עברו** ל-**Protected with Play → Play Store protection**.
- **שדה Package name קיים בטופס יצירת האפליקציה** ונדרש למילוי ידני — אין להסתמך על כך שהוא ייקרא מה-`.aab`.
- ⚠️ **"Start rollout" אינו פרסום.** אחרי הרולאאוט הגרסה עדיין מציגה **"Not yet sent for review"** ויושבת כטיוטה — שום דבר לא מגיע לבודקים, והשעון לא זז. חייבים ללחוץ **Go to Publishing overview → Send changes for review**. קל מאוד להחמיץ את זה ולחשוב שסיימת.
- **שם הגרסה ותגיות השפה כבר ממולאים.** Play מציעה `2 (1.0.1)` (קוד גרסה + שם) — עדיף על כל דבר שנקליד ידנית, כי היא מאשרת שהקובץ הנכון עלה. וב-Release notes התגיות `<iw-IL>` כבר בתיבה; רק להחליף את שורת ההסבר שביניהן. **המונה "Release notes provided for N language" הוא האישור שזה נקלט.**
- **שתי אזהרות שיופיעו בכל בנייה ואינן דורשות דבר:** ירידה במספר המכשירים הנתמכים (מ-minSdk 23 ל-24), ו-"no deobfuscation file" — שמופיעה דווקא מפני שהקוד **אינו** מעורפל (`minifyEnabled false`). **אזהרות צהובות אינן חוסמות; רק שגיאות אדומות חוסמות.**
- **בשדות שם קובץ באנגלית, נקודה מובילה מתבלבלת בקלות.** משתמש שחושב בעברית עלול להקליד `nojekyll.` במקום `.nojekyll`. להנחות לפי **סדר הקלדה** ("קודם מקש הנקודה, ואז האותיות"), אף פעם לא לפי "בהתחלה" או "בסוף".

#### דרישת ניסוח בתיאור החנות — לא למחוק

בטופס Health apps **לא** הצהרנו על מכשיר רפואי. גוגל דורשת שהתיאור בחנות יכלול במפורש שהאפליקציה **אינה מכשיר רפואי ואינה מאבחנת, מטפלת, מרפאה או מונעת מצב רפואי**, ושיש לפנות לאיש מקצוע רפואי. פסקת "חשוב לדעת" בתיאור היא **דרישת מדיניות, לא נימוס** — אין למחוק אותה.

בטופס **Data safety** הוצהר "לא נאסף שום מידע", וזו אמת: ההגדרה של גוגל ל"איסוף" היא העברת מידע אל מחוץ למכשיר, ו-`localStorage` מקומי אינו כזה. **כל פיצ'ר עתידי שישלח משהו לשרת — כולל אנליטיקה — מחייב עדכון של ההצהרה הזו ושל מדיניות הפרטיות.** הצהרה לא מדויקת שם היא עילה להסרת האפליקציה.

#### חשבון פרטי לא יכול לפרסם אפליקציה רפואית — זה עלה לנו בשתי דחיות

מאז אוגוסט 2024, חשבונות מפתח **פרטיים** חסומים מלפרסם, בין היתר, **Health apps "such as Medical apps"**. הדרישה היא חשבון **ארגוני** — ישות משפטית רשומה ומספר D-U-N-S. **ההחלטה בפרויקט הזה: לא להקים ישות משפטית.** לכן האפליקציה חייבת להישאר מסווגת כלא-רפואית, וזה מחייב **שני** דברים שקל להתבלבל ביניהם:

| איפה | מה חייב להיות |
|---|---|
| **Store settings → App category** | `Health & Fitness` |
| **App content → Health apps → תכונות** | `Other`, עם תיאור חופשי של מעקב מלאי. **לא** `Medication and treatment management` |

**המלכודת:** שינוי של קטגוריית החנות בלבד **לא מספיק**. בסיבוב הראשון שינינו רק אותה, השארנו את `Medication and treatment management` מסומן בהצהרה — והאפליקציה נדחתה שוב עם נימוק **זהה**. גוגל קוראת את ההצהרה, לא רק את הקטגוריה, ו-`Medication and treatment management` יושב תחת הקבוצה **Medical**.

**וזה מדויק, לא התחמקות.** ההגדרה של גוגל ל-Medical היא "מספקת מידע רפואי, משאבים או כלים לשיפור טיפול רפואי, אבחון וטיפול", ושל הקטגוריה הזו בפרט — "לוחות זמנים לנטילה, שירותי בית מרקחת, היענות לטיפול, מזהי כדורים ותזכורות נטילה". **Supplever לא עושה אף אחד מאלה** — היא סופרת יחידות ומייצרת רשימת הזמנה. אין תזכורות נטילה, אין מעקב היענות, אין מידע רפואי.

**מסקנה מעשית:** כל פיצ'ר עתידי שיוסיף **תזכורת ליטול תרופה** או **מעקב היענות** יהפוך את האפליקציה לרפואית בעיני גוגל, ויחייב חשבון ארגוני. זה שיקול מוצרי, לא רק רגולטורי.

**ולערעור יש כדור אחד:** גוגל מאפשרת **הגשה אחת** לכל החלטת אכיפה (Policy status → Get support), עד 1000 תווים, ונענית רק באנגלית. הגשה חוזרת של גרסה אחרי תיקון, לעומת זאת, **אינה מוגבלת** — ולכן תמיד לתקן ולהגיש מחדש קודם, ולשמור את הערעור לסוף.

#### נכסי דף החנות

צילומי המסך והבאנר (1024×500) נמצאים בתיקייה `צילומי מסך לחנות`, במקביל לתיקיית הפרויקט. **הם נוצרו מנתוני דמו ולא ממלאי אמיתי** — אין להחליף אותם בצילומים מהטלפון האישי, כי דף החנות ציבורי ורשימת התרופות היא מידע רפואי.

הם נוצרו ברינדור של Edge ללא ממשק, ברוחב חלון 640 ובהגדלה כפולה. **שתי מלכודות שם:** רוחב חלון קטן מ-500 פיקסלים בדפדפן ללא ממשק מרנדר רחב יותר מהחלון וחותך — וב-RTL החיתוך נופל דווקא על **תחילת** התוכן; וצילום מסך רגיל מטלפון מודרני (20:9) **נדחה** ע"י גוגל, שדורשת שהצלע הארוכה לא תעלה על פי שתיים מהקצרה.

## מי בונה את זה

המשתמש בונה את הפרויקט **לבד**, **ללא רקע כמפתח מקצועי**. המשמעות עבורי (קלוד):
- להעדיף פשטות על פני "best practices" שדורשות תשתית מסובכת.
- להסביר החלטות טכניות בשפה ברורה, לא להניח היכרות מוקדמת עם כלים/ז'רגון.
- להימנע מלהכניס כלים, ספריות או תבניות ארכיטקטורה שלא באמת נחוצות ל-MVP.
- ללכת צעד-צעד, ולוודא בכל שלב שהדבר עובד בפועל (הרצה בדפדפן) לפני שממשיכים הלאה.

## החלטות מוצר שכבר התקבלו

| נושא | החלטה |
|---|---|
| פלטפורמה | אפליקציית ווב / PWA (לא native mobile) |
| מודל עסקי | חינמי לגמרי, ללא תשלום/מנוי |
| התראות | התראות מקומיות של המכשיר בלבד (לא Push שרת) |
| סנכרון בין מכשירים | לא נדרש בשלב זה — מכשיר אחד מספיק |
| ריבוי משתמשים/מטופלים במכשיר | טרם הוכרע — ראו "החלטות פתוחות" |

### למה זה חשוב לארכיטקטורה

מכיוון שאין צורך בסנכרון בין מכשירים ואין Push מהשרת — **אין סיבה אמיתית ב-MVP לבנות backend/שרת בכלל**. אפשר לבנות אפליקציה **local-first**:
- הנתונים נשמרים על המכשיר (IndexedDB / localStorage).
- התראות דרך ה-Notification API + Service Worker (רץ ברקע גם בלי שהאפליקציה פתוחה, כל עוד היא מותקנת כ-PWA).
- בלי שרת, בלי בסיס נתונים מרוחק, בלי אימות משתמשים (auth) — כל זה נחיסך ב-MVP.
- אם בעתיד יתווספו סנכרון/ריבוי מכשירים/שיתוף עם בן משפחה — זה יהיה שכבה נוספת שנוסיף מעל, לא שינוי ביסוד.

**המלצה טכנית (ניתנת לשינוי):** HTML/CSS/JS פשוט, בלי build step מורכב (או Vite קליל אם ירצה מבנה פרויקט מסודר), כדי שיהיה קל להריץ, להבין ולתחזק בלי ניסיון פיתוח קודם. הקונספט הקיים כבר כתוב כך.

## פיצ'רים מרכזיים (MVP), מתוך עמוד הקונספט

1. **הוספת תרופה** — שם, מינון (אופציונלי), כמות יחידות בקופסה, **תדירות נטילה** (ראו למטה), מלאי קופסאות נוכחי, ומלאי יחידות נוכחי. מלאי היחידות מחושב **אוטומטית** מקופסאות × יחידות-בקופסה, אבל השדה נשאר ניתן לעריכה ידנית (למשל אם קופסה אחת חלקית).
2. **מעקב מלאי אוטומטי** — המערכת מחשבת לבד כמה ימים נשארו לפי המלאי וקצב הנטילה.
3. **התראות מוקדמות מותאמות אישית** — 6 אפשרויות קבועות בלבד: שבוע / שבועיים / שלושה שבועות / חודש / חודשיים / שלושה חודשים (לא טווח חופשי). הבחירה בפעם הראשונה נשמרת כברירת מחדל אוטומטית לתרופות הבאות שיתווספו.
4. **רשימת הזמנות מרוכזת** — כל התרופות שהגיע זמנן עוברות לרשימה אחת, מוכנה מול בית המרקחת.
5. **עדכון מהיר אחרי קבלה** — מסמנים כמה קופסאות התקבלו והמלאי מתעדכן לבד, בלי חישוב ידני.
6. **עבודה אופליין** — כל המידע על המכשיר; ללא תלות בחיבור לאינטרנט.
7. **דשבורד צבעוני וברור** — סטטוס ירוק/צהוב/אדום לפי דחיפות ההזמנה, מבט אחד מספיק.

## זהות עיצובית (מתוך supplever-cover.html)

**הלוגו (טבעת קפסולות SVG) והפלטה משותפים בפועל** בין עמוד הקונספט לבין האפליקציה עצמה — אותו SVG מופיע ב-header של [index.html](index.html), ואותם משתני CSS (כולל `--capsule-cap`/`--capsule-body`) מוגדרים ב-[style.css](style.css). אם עורכים את הלוגו, לערוך בשני המקומות.

- **גופנים:** כותרות — `Frank Ruhl Libre` (serif); טקסט רגיל — `Heebo` (sans-serif). RTL, עברית.
- **פלטת צבעים עיקרית:**
  - רקע: `#EAEFE7` (ירקרק בהיר), רקע עמוק יותר `#DEE6DA`
  - טקסט: `#182720` (כהה), משני `#51625A`
  - צבע מותג ראשי: טיל כהה `#1F4A43` (עם גוון בהיר `#2E6259`)
  - צבע משני/אקסנט: חימר כתום `#D98F4E` (כהה יותר `#B96F31`)
  - סטטוסים: טוב `#4C7A5E`, אזהרה `#D9A441`, סכנה `#C1503A`
- **סמל:** קפסולה/כמוסה, מסודרות במעגל כלוגו.
- **טון:** רגוע, חם, לא קליני-קר — מרגיש כמו "שקט נפשי" ולא כמו אפליקציה רפואית סטרילית.

כדאי לשמר את השפה הזו כשבונים את מסכי האפליקציה עצמה, כדי שהיא תרגיש המשך טבעי לעמוד הקונספט.

**סטייה מודעת מעמוד הקונספט:** בעמוד הקונספט המשפט "תמיד צעד אחד לפני גמר המלאי" צבוע כתום (`--clay-dark`). באפליקציה עצמה (בכותרת, מתחת ל-"Supplever") אותו משפט צבוע **ירוק/טיל** (`--teal-deep`) — זו בקשה מפורשת של המשתמש, לא טעות/חוסר עקביות שצריך "לתקן" בהתאם לעמוד הקונספט.

**אייקון מסך ריק** ב"המלאי שלי" (כשאין תרופות) הוא SVG קפסולה מותאם אישית עם גרדיאנטים לנפח תלת-ממדי קל וצל רך — לא אימוג'י 💊 (הוחלף לבקשת המשתמש כי לא ניתן לצבוע אימוג'י בצבעי המותג).

## החלטות פתוחות (טרם הוכרעו — לבדוק בהמשך)

- **ריבוי פרופילים במכשיר אחד** — האם מטפל משפחתי צריך לנהל כמה "מטופלים" נפרדים באותה אפליקציה? עדיין לא סוכם.
- **שם קבוע לתיקיית הפיתוח** — כרגע כל הקוד/עמוד הקונספט יושבים ישירות תחת `Application Development`; מבנה תיקיות מפורט (src/, assets/ וכו') ייקבע אם האפליקציה תגדל.

**כבר הוכרע בפועל (לא רק המלצה):**
- vanilla HTML/CSS/JS בלי build step, בלי framework — זה מה שנבנה ועובד.
- Hosting: GitHub Pages, חינמי, מריפו ציבורי [github.com/alongut291-arch/Supplever](https://github.com/alongut291-arch/Supplever). כדי לעדכן את האתר החי צריך `git add` + `git commit` + `git push origin main`.

## איך לעבוד איתי על הפרויקט הזה

- זה פרויקט פרטי, לא מסחרי (לפחות בשלב זה) — אין צורך בתשתית ל"סקייל" גדול.
- להעדיף לבנות דבר אחד שעובד מקצה לקצה (למשל: הוספת תרופה → חישוב ימים → התראה) על פני הרבה פיצ'רים חלקיים.
- keep it simple: אם יש שתי דרכים לפתור בעיה ואחת פשוטה יותר להבנה ותחזוקה על ידי מי שאינו מפתח מקצועי — לבחור בה, גם אם היא "פחות אלגנטית" מבחינה הנדסית.
